name = "OpenCommerceLens Web"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "pnpm run migrate:shopify-storage && pnpm run migrate:webhook-queue"
dev = "pnpm exec react-router dev"
