import { Check, Loader2 } from 'lucide-react';
const steps=['Approval received','Payment authorized','Creating merchant orders','Waiting for confirmations','Orders confirmed'];
export default function CheckoutProgress({step,orders}){return <div className="p-8 max-w-3xl mx-auto"><h1 className="text-2xl font-semibold">Completing your order</h1><div className="panel p-6 mt-6">{steps.map((x,i)=><div key={x} className={`flex gap-3 py-3 text-sm ${i<=step?'text-foreground':'text-muted-foreground'}`}>{i<step?<Check className="text-emerald-500"/>:i===step?<Loader2 className="animate-spin text-primary"/>:<span className="w-6"/>}{x}</div>)}</div><div className="grid sm:grid-cols-3 gap-3 mt-5">{orders.map((o,i)=><div className="panel p-4" key={o}><p className="text-sm font-medium">{o}</p><p className="text-[10px] text-primary mt-1">{step>2?'Confirming…':'Ready'}</p></div>)}</div></div>}