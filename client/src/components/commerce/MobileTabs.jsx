import { MessageCircle, LayoutGrid, Sparkles, ShoppingBag } from 'lucide-react';
const tabs=[['chat','Chat',MessageCircle],['results','Results',LayoutGrid],['tryon','Try-on',Sparkles],['checkout','Checkout',ShoppingBag]];
export default function MobileTabs({mode,tab,onSelect}){return <nav className="md:hidden h-16 border-t border-white/10 bg-card flex justify-around">{tabs.map(([id,label,Icon])=>{const active=id==='chat'?tab==='chat':tab!=='chat'&&(id==='results'?['results','product','comparison','searching'].includes(mode):id==='tryon'?mode==='tryon':['checkout','processing','confirmation'].includes(mode));return <button key={id} onClick={()=>onSelect(id)} className={`grid place-items-center text-[10px] transition ${active?'text-[#3b82f6]':'text-[#737373]'}`}><Icon size={18}/>{label}</button>})}</nav>}