import { ArrowLeft, Heart, Scale, Sparkles, ShoppingBag } from 'lucide-react';
export default function ProductDetail({product,onBack,onMode}){return <div className="p-6 overflow-y-auto h-full max-w-4xl mx-auto"><button onClick={onBack} className="control mb-5"><ArrowLeft size={15}/>Back to results</button><div className="grid md:grid-cols-2 gap-8"><img src={product.image} alt={product.name} className="w-full max-h-[65vh] object-cover rounded-[28px] shadow-elevated"/><div><p className="text-xs text-primary">{product.match}</p><h1 className="text-3xl font-display mt-1">{product.name}</h1><p className="text-muted-foreground mt-1">{product.merchant}</p><p className="text-xl mt-4 font-medium">${product.price}</p><p className="text-sm text-muted-foreground mt-5">A softly structured cropped jacket with a wide collar and clean finish, selected for its close visual match.</p><label className="label">Colour</label><div className="flex gap-2"><button className="swatch bg-black ring-2 ring-primary"/><button className="swatch bg-amber-800"/></div><label className="label">Size</label><div className="flex gap-2">{['XS','S','M','L','XL'].map(x=><button key={x} className={`icon-control ${x==='M'?'border-primary ring-2 ring-primary':''}`}>{x}</button>)}</div><div className="flex flex-wrap gap-2 mt-8"><button className="control"><Heart size={15}/>Save</button><button onClick={()=>onMode('comparison')} className="control"><Scale size={15}/>Compare</button><button onClick={()=>onMode('tryon')} className="primary"><Sparkles size={15}/>Try on</button><button onClick={()=>onMode('checkout')} className="primary"><ShoppingBag size={15}/>Buy</button></div></div></div></div>}