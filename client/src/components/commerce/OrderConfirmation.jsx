import { CheckCircle2, ExternalLink } from 'lucide-react';
export default function OrderConfirmation({orders,onMode}){const confirmed=orders.filter(o=>o.status==='confirmed');return <div className="p-8 max-w-4xl mx-auto overflow-y-auto h-full"><CheckCircle2 className="text-emerald-500" size={38}/><h1 className="text-3xl font-display mt-3">{confirmed.length===orders.length?'Your orders are confirmed':'Some orders need attention'}</h1><p className="text-sm text-muted-foreground mt-1">{confirmed.length} of {orders.length} merchant orders confirmed.</p><div className="grid md:grid-cols-3 gap-4 mt-6">{orders.map(o=><article key={o.orderId} className="panel p-5"><p className="text-xs text-emerald-500">● {o.status}</p><h2 className="mt-2 font-medium">{o.merchantName}</h2><p className="text-xs text-muted-foreground mt-3">Order {o.orderId}</p><p className="text-xs">${o.amount.toFixed(2)} · Delivery {o.eta}</p><button className="control mt-4">View order <ExternalLink size={13}/></button></article>)}</div><button onClick={()=>onMode('results')} className="primary mt-6">Continue shopping</button></div>}