import MerchantCheckoutGroup from './MerchantCheckoutGroup';import OrderSummary from './OrderSummary';import PravaApproval from './PravaApproval';
export default function Checkout({items,setItems,onApprove,onBack}){const groups=items.reduce((a,x)=>({...a,[x.merchant]:[...(a[x.merchant]||[]),x]}),{});return <div className="p-5 h-full overflow-y-auto max-w-5xl mx-auto"><h1 className="text-2xl font-semibold">Review your order</h1><p className="text-xs text-muted-foreground mb-4">{items.length} items from {Object.keys(groups).length} stores</p><div className="grid lg:grid-cols-[1fr_300px] gap-4"><div className="space-y-4">{Object.entries(groups).map(([name,list])=><MerchantCheckoutGroup key={name} name={name} items={list} onRemove={id=>setItems(v=>v.filter(x=>x.cartId!==id))} onEdit={()=>{}}/>)}</div><aside className="panel p-5 h-fit"><OrderSummary items={items}/><PravaApproval onApprove={onApprove} onBack={onBack}/></aside></div></div>}